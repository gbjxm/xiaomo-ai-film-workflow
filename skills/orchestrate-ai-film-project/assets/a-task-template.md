## 当前任务

当前状态：待定

交互前台：

目标岗位（A 权威执行目标）：

当前任务版本 / `A_总控.md` SHA-256：

一句话任务：

阶段目标：

阶段内可直接处理的小幅修改：

必须返回 A 的重大变化：

增量输入与关联 ID：

当前对象 / 精确版本 / SHA-256：

受影响执行坐标：

本次不做（首次分派、跨岗位或变化时填写）：

完成标准：

完成后写回（变化时填写）：

需要另行授权的外部动作：

后台所有权微补丁：普通任务省略；仅触发时展开“来源任务 ID / 文件所有者 / 稳定 ID 旧→新版本 / 新 SHA-256 / 用户采用决定定位 / 记录写入授权定位 / 原子写入范围 / 受影响执行坐标 / 同步状态”。A 权威执行目标保持为交互前台，不展开暂离/恢复。

暂离/恢复：普通阶段任务省略；只有发生合法暂离或返回时，另行展开“来源任务 / 返回岗位 / 恢复对象与精确版本 / 恢复条件 / 往返次数”。

## 当前有效指针

后期预设或默认 Profile：

C 交 D 素材清单路径 / 版本 / SHA-256：

D 接管快照路径 / 版本 / SHA-256：

采用 CUT 与 Picture Lock：

Master 版本 / 路径 / SHA-256 / Output Profile：

PIC / AUD / TEXT / GFX 版本与状态：

QC / 限制 / 恢复 / 权利证据路径：

## 最近交接

来源岗位：

已完成：

证据路径：

未决问题：

唯一下一步：
