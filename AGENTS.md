# 工作流维护规则

## 工作室协议与产品版本路由

- 先读项目入口。仅 workflowStudio: studio-v0.9 时使用工作室共同契约和能力文件；V0.9 分支优先于下文旧五岗位、A唯一当前岗、同岗缓存和逐门写回规则。无标识项目继续下文原有兼容规则，不因安装版本而升级。
- V1 产品沿用 studio-v0.9 协议和现有 runtime schema；新入口另有唯一 workflowRelease: 1.0。无该发布标识的旧项目保持原分支，未知或重复值拒绝执行；V1 原生图片分支仅在实际工具可用及本次执行授权充分时适用，media_execution 仍为 false。
- 工作室为 A/B/C1/C2/D/E 六部门：A 管制作组织，C2 从前期贯穿整片创作，C1 管资产；部门不是永久聊天。完整候选自动保存，明确采用后所属专业提交；只在用户要求新开或续接时创建可见任务。
- 专业共同契约：skills/orchestrate-ai-film-project/references/studio-v09-contract.md；文本保存与恢复：同目录 studio-runtime-v1.md。日常项目不读取维护历史，不启动旧 same_role_resume 脚本。
- V0.9 仅调用已启用能力。旧项目接入保留剧本、素材、影视编号、采用版本和真实状态，安装与项目接入分别保留恢复点。候选、采用、软件验证、真实媒体效果和发布状态分别记录。
- 下文维护流程、写前复核、安装隔离验证、逐批验证、知识工程冻结及外部动作授权对各版本均有效；旧五岗位细则仅约束未启用 V0.9 的项目。

## 当前活动基线

- 产品版本：V1（bundleVersion 1.0）；工作室协议仍为 studio-v0.9。
- 总框架：`工作流/小陌AI影视工作流总框架_v1.0.md`。
- 权威项目模板：`工作流/项目模板/小陌AI影视项目模板_v1.0/`。
- 活动版本区只保留当前版 v1.0 和上一版 v0.9；v0.8 及更早版本由原工作区原样冷归档，不随本仓库提供，不删除或迁移依赖项目。
- 旧项目的来源版本与迁移记录以使用者本机材料为准；个人项目和冷归档不随 GitHub 发布副本提供。
- 活动 Skill 集以 `skills/manifest.json` 为唯一清单，共 23 个；`organize-ai-film-media` 为跨 C/D 的生成视频整理工具，不新增部门；D 保持一个 `edit-and-finish-ai-film` 主入口，按需调用 `grade-film-in-resolve` 专业调色子能力和 `davinci-resolve-mcp` 技术接口，不增加部门或正式记录所有者。旧 `prompt-generator` 继续停用。
- B 保留两个故事主入口；`evaluate-film-topics` 为选题、类型与影片参照子能力，热点查询默认只报近期热度，五平台为抖音、B站、小红书、TikTok、YouTube，不新增部门或记录所有者。
- `improve-ai-film-workflow` 独占各项目 `流程改进日志/`；`manage-film-competition-submissions` 为五岗位之外的独立投稿管理 Skill。
- `00_开始这里.md` 是工作区入口；项目建立后，项目内 `开始这里.md` 是唯一日常入口。

## 新机器安装接管

- 当用户在一台已有 Codex 的新机器上明确要求安装当前完整工作流文件夹时，该请求授权安装 `skills/manifest.json` 列出的随包 Skills并写入本机工作流路径配置；不授权项目迁移、项目内容修改、媒体生成、剪辑、上传或发布。
- 先读取根目录 `00_开始这里.md`、`skills/manifest.json` 和 `安装工作流.ps1`，确认当前目录结构完整；不从旧机器路径或历史摘要猜测安装位置。
- 在根目录运行 `powershell -NoProfile -ExecutionPolicy Bypass -File ".\安装工作流.ps1"`，成功后再运行同一命令并追加 `-VerifyOnly`。以安装器返回的配置路径、安装路径、manifest 数量和校验结果为准。
- 23 个活动 Skills 已包含在工作流文件夹内，不联网搜索、下载或替换其他版本。安装后由 Codex 按任务触发，不要求预先加载全部 Skill 正文；技能安装不代表达芬奇软件/API/MCP 已可用。
- 安装失败时保留安装器的事务备份和回滚结果，只报告精确阻塞；不得为完成安装而修改 `项目/`、补迁移旧项目或扩大到外部工具安装。

## 上下文与 Token 预算

- 项目岗位首次进入一个任务时，只读取项目 `开始这里.md` 的共享契约和本岗位启动词，再读取 A 当前任务、本岗位主文件和 A 点名的最小证据；跳过其他岗位启动词。
- 同一任务内已经读取且没有外部修改迹象的共享契约、A 当前任务和岗位主文件直接复用。
- 后台所有者使用轻量模式，只读请求、目标对象/索引、预期 hash 和不变量；默认不加载常规专业 references，除非差量本身确需新的专业判断。
- 合格机械小改默认由当前一次回复内的受限岗位适配器完成，不新建代理/任务；只有需要新的专业判断或运行时无法保证写入白名单时，才降级到既有所有者任务或受限子任务。
- 后台微补丁只传“来源任务 ID、A 当前任务版本/hash、用户采用证据、记录写入授权证据、对象旧版/hash、差量、不变量、受影响执行坐标、失效范围、外部动作授权、恢复条件”，不复制整份岗位文件或完整聊天；默认一名文件所有者、一轮完成。
- 草案、咨询、诊断和纯只读检查不计算文件 SHA-256。用户明确采用并即将写入时，先重读目标并记录 SHA-256，紧邻写入前再核对；值变化则停止。
- 搜索从 A 点名的 ID、文件和活动目录开始；冷归档、升级候选和隔离运行目录不是日常事实源。
- 新项目首轮只建立五岗位任务拓扑；B/C/D/E 轻量待命，下一条消息才读取业务上下文。标题已正确时不重复调用重命名工具。

### 跨任务同岗续接

- 只有项目入口声明 `workflowResume: same-role-resume-v1` 时，B/C/D 的新聊天才启用同岗续接；无标识项目继续使用原有首次进入规则，不补缓存、不迁移。
- 新聊天先读取 `开始这里.md` 的共享契约、本岗位启动词、`A_总控.md` 的 `## 当前任务` 区段和 `续接缓存/<岗位>_同岗续接.json`。缓存与 A 门禁有效时，只加载缓存中的 `requiredReadSet` 和当前执行游标；阶段包络不是启动读取列表。
- 同岗续接缓存是可删除、可重建、最大 6,144 UTF-8 字节的非权威运行缓存。它可以保存采用状态的精确指针和显式标为待定的工作游标/草稿指针，但不取代 A、岗位主文件、Word、场景卡、CUT、Master 或真实媒体；写缓存不等于采用、正式岗位写入或外部动作授权。
- 已启用项目把“当前对象、待定差量、下一步、授权、依赖或失效门发生变化时自动刷新本岗位缓存”视为独立的常驻运行缓存授权。不得写入完整聊天、隐藏推理、图片/音视频内容、大段专业正文或项目外隐私；无变化不重复写。
- 同岗冷启动允许只读核验缓存点名的少数 whole/Markdown H2-section/唯一索引行 SHA-256；这是只读恢复例外，不替代正式写入前的二次哈希，也不授权递归计算目录、候选素材树或媒体库哈希。
- 默认不读取旧任务历史。缓存缺少且存在未持久化工作时，最多读取同项目同岗位最近 2 个已完成任务摘要，总输出上限 8 KB；禁止读取旧工具长输出。仍不能唯一恢复时只问一个决定性问题。
- 用户明确说“切换到新的 B/C/D 同岗续接任务”时，当前岗位才可准备缓存并创建同一项目的 `岗位｜续接NN` 本地任务。创建成功并激活新租约后，旧任务进入 superseded，只能查看或交接，不得继续写项目；创建失败则取消 pending handoff 并保留旧租约。
- 用户也可手动新建同岗位任务并发送固定短提示词；新任务通过 revision/lease 领取缓存。若另一任务仍持有未过期 lease，只有当前用户消息明确切换且已确认旧任务停止/空闲时才可接替；无法确认时停止并改走旧任务 `PrepareHandoff → ActivateHandoff`。任一任务写缓存或岗位文件前必须匹配当前 revision 和 lease；不匹配时零写入停止，避免新旧聊天并发覆盖。
- 缓存漂移按对象依赖局部失效；路径缺失、格式损坏或无法唯一定位时走区段级恢复，不得退化成全项目扫描或自动选择“最新文件”。

## 五岗位与共同契约

- A 只调度、记录恢复指针与验收；B 负责故事；C 负责视觉生产；D 负责后期完成与影片本体 QC；E 负责发行包装、检查、回执和真实数据复盘。
- 只有项目入口声明 `workflowScript: b-word-stage-ready-v1` 时，才启用下述共享 B 完成门、Word 协同目录与阶段完成包；未声明的既有项目继续按原入口运行，不自动迁移 B/A 主文件或历史正文。
- B 故事阶段的完成标准由 A/B 共用的 `B_STAGE_READY v1` 唯一契约定义。B 按该契约执行专业且详细的创作、正文、制作与证据自检；未通过时留在 B 内继续。A 只检查缺项、当前版本/hash、用户决定、阻塞项、Sxx 范围与任务包络，不重新逐场审查对白、人物、因果和其他创作细节；完整性通过后直接派 C。
- 剧本进入对白、场景动作、故事层连续性、计时或最终文字精修时，可启用 Word 协同。Codex 只以已保存文件为同步点；用户继续编辑时，Codex 对保存快照制作修订/批注副本，不能声称看见未保存文字，也不能覆盖用户未同步内容。分支、快照和历史由后台管理，用户不承担手工版本管理。
- B 只在活跃区保存当前剧本工件指针、版本/hash、Sxx 锚点、冲突与完成检查；旧全文进入 `剧本/历史/` 且默认不读。B 的故事层场记只描述场景事实、人物状态和前后场衔接；服化道、镜头、生成素材等生产连续性仍归 C。
- 稳定 ID 只使用 S/A/V/CUT/PUB；Setup、batch/take、PIC/AUD/TEXT/GFX 与 Master 是执行坐标、组件或版本。
- B/C/D/E 的普通追问、局部返修、版本迭代和同岗 Skill 切换留在当前阶段。
- 用户所在的“交互前台”与 A 的“权威执行目标”是两个概念；未触发后台路由时二者相同。微补丁保持 A 目标为前台岗位，文件所有者只以受限后台子操作写自己的文件；重大串行路由则由 A 改变权威执行目标，原交互前台进入暂停，恢复前不得继续使用旧输入。
- 跨岗位但稳定 ID、制作功能、阶段目标、任务包络与授权类别均不变的小改，只有同时具备用户采用证据和记录写入授权证据时，才走“单一前台＋后台所有权微补丁”：所有者后台独占写入；A 只同步确实失效的精确指针；原岗位重绑后续办。不得要求用户手工切任务或复制内部交接。
- 微补丁最多涉及当前岗位与一名文件所有者，必须有来源/A 指纹、预期 SHA-256、精确差量、不变量、受影响执行坐标、失效范围和一轮恢复条件；目标并发变化、影响扩到第二个上游岗位或一轮不能闭合时停止并升级 A。岗位独占写入始终不变。
- 新增/删除场景或稳定对象、改变核心事件/动机/关系结果/结局/制作功能、CUT 结构目标、Output Profile、平台或主要传播承诺，或触及已锁定下游、里程碑、权利/安全及新外部动作授权，仍属于重大变化，由 A 决策。用户明确称为重大修改时不得降级为微补丁。
- `$direct-ai-scene-production` 是 C 阶段中控：每个采用 Sxx 最多一张场景制作卡，按需选择六站，汇合 Vxx Setup，并形成精确 C 交 D 素材清单；它不是第二个 A。
- D 保持一个 `$edit-and-finish-ai-film` 主 Skill，专业 references 和专项能力按任务读取；默认通过经实际核验的达芬奇后台原生 API/MCP 工作，不占键鼠。`grade-film-in-resolve` 负责调色判断，`davinci-resolve-mcp` 负责技术接口，D 主入口与所有权不变。C 交 D 清单与 D 接管快照分别由 C、D 独占；A 只保存当前有效路径、版本与 hash 指针。
- CUT 创建、采用、Picture Lock、组件冻结、Master 绑定与 Master QC 分门；组件变化不得误升 CUT。
- D 交 A 必须含采用 CUT vNN、Lock、精确 Master path/version/hash/Profile、四组件状态、自动＋人工 QC、限制、恢复和权利证据。A 验收后才可派 E。
- E 只消费精确 QC Master；新画幅、重编码、字幕/音轨版本或修复须经 A 返回 D，E 不改母版。
- 公开发布前必须明确是否保留首映或投稿资格；未确认不得判就绪，需要时交 A 调用比赛管理 Skill。

## 兼容与强制边界

- 机器安装 v0.8 不代表项目迁移。公共 Skill 必须从项目入口和真实文件结构判断版本，不能只看机器配置。
- “单一前台＋后台所有权微补丁”只在项目入口声明 `workflowPatch: single-foreground-micropatch-v1` 时启用；没有标识的既有 v0.8 项目继续按原跨岗位规则运行，单独授权同步后才启用。
- `B_STAGE_READY v1` 与 Word 协同只在项目入口声明 `workflowScript: b-word-stage-ready-v1` 时启用；机器 Skill 更新不代表项目脚本治理迁移。既有项目未同步入口时不得补 `剧本/` 目录、移动 Word 或清理 B 历史全文。
- 已经完成 B 且 A 正在 C/D/E 的既有项目可前瞻性启用该标识：不追溯要求旧 B 重做完成包，不让既有锁定 Sxx、Word 或当前下游任务失效；下一次完整 B 阶段正常完成时才首次强制 `B_STAGE_READY`。C 阶段中的单个 B 微补丁仍按既有微补丁契约，不冒充一次完整 B→A 交接。
- v0.75/v0.7/v0.6/v0.5 项目不得自动迁移、补目录或改历史报告；旧项目无 E 时保持零写入。
- 修改活动框架、模板、Skills 或机器安装副本，必须限定在用户当前授权范围内并逐批验证。
- `工作流/` 活动版本区始终只保留当前版和紧邻上一版；更早版本若被项目依赖，只能原样冷归档，不能删除。
- 升级备份、候选、迁移证据和验收报告不等同于活动版本，不随版本轮换自动删除。
- 未获项目迁移授权，不得修改、移动、重命名或删除 `项目/` 中任何既有项目。
- 维护、评测和安装测试必须使用隔离目录，不得把正式项目当测试夹具。
- 当前入口、脚本、框架、模板和 Skills 使用相对定位或本机安装配置，不写死工作流根目录。
- `知识工程/` 整体冻结；外部原书不属于便携工作区。
- 生成媒体、执行剪辑、封面媒体制作或改写、上传和公开发布分别需要明确外部动作授权。
- V0.9 `media_execution` 仍指工作室运行时内置媒体功能，不因本次达芬奇接入全局打开。独立达芬奇原生工具须实际可用并有本次用户执行授权；默认在同一工程持续调整用户指定的一版，仅明确测试时制作原版与三个候选。安装不迁移既有项目，不扩大 C 生成、上传或发布权限，也不证明全部 API 或真实效果已验证。
- 自动路由与文本/记录写入不得扩张上述授权；后台调用所属岗位不等于已经生成、剪辑、转码、改图、上传或公开。

## 修改流程

1. 先读 `HANDOFF.md`，再以实际文件、哈希、日志和测试为准。
2. 一个 Skill 聚焦一个职责；通用契约在 Skill 中只说明一次，细节按需放入 references。
3. 修改框架、模板或 Skill 时按影响面验证；发现回归只回退当前批次。
4. 写入前复核目标 SHA-256，发现并发变化立即停止。
5. 重要里程碑更新 `HANDOFF.md`；最终证据写入维护区验收报告。未来版本继续在隔离候选区开发，用户确认后再单独晋升。
6. 项目中的普通流程问题只由 `$improve-ai-film-workflow` 写入 `流程改进日志/`；查看日志零写入，修改必须点名完整 WFI ID 并获得明确授权。
7. 版本轮换前扫描项目来源版本；仍有项目依赖时只能冷归档。轮换后核对活动版本数、归档哈希、项目哈希和所有活动入口引用。

## 验证

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File ".\安装工作流.ps1"
powershell -NoProfile -ExecutionPolicy Bypass -File ".\安装工作流.ps1" -VerifyOnly
```

- 修改一个或数个 Skill：对受影响 Skill 运行 `quick_validate.py` 和对应行为回归；修改 manifest、安装器或正式晋升全套 Skill 时校验全部 23 个。
- 修改权威模板或启动器：运行隔离启动器测试，必须带 `-NoOpen` 和隔离 `-ProjectsRoot`、`-TemplateRoot`。
- 修改安装器或正式安装同步：测试时隔离 `-InstallRoot`、`CODEX_HOME` 与 `USERPROFILE`；正式安装后核对 manifest 23 个源码与安装副本哈希。
- Windows 中文 `.ps1` 保持 UTF-8 BOM；PowerShell 保持 `$ErrorActionPreference='Stop'`。
