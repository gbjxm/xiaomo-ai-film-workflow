# 小陌 AI 影视工作室 V1｜开始这里

这是可搬移的 Windows 工作流包。保留目录结构即可，不要求相同盘符；项目建立后，项目内“开始这里.md”是唯一日常入口。

## 新电脑安装与路径更新

把整个工作流文件夹复制到新电脑，在 Codex 中打开这个文件夹并发送：

```text
请读取根目录 AGENTS.md、00_开始这里.md、skills/manifest.json 和 安装工作流.ps1，安装随包23个Skills并验证本机路径配置。不要迁移或修改现有项目。
```

安装与只读复核：

```powershell
powershell -NoProfile -ExecutionPolicy Bypass -File ".\安装工作流.ps1"
powershell -NoProfile -ExecutionPolicy Bypass -File ".\安装工作流.ps1" -VerifyOnly
```

安装器以 manifest 的版本选择权威模板，写入本机工作流路径配置；已有同名不同内容的 Skill 先备份再替换。只处理清单中的 23 个 Skills，不另行下载。移动工作流文件夹后重新安装以更新路径。旧 prompt-generator 保持停用。技能安装不代表达芬奇软件、API/MCP 连接或全部专业功能已验证可用。

个人知识树是可选外部能力，不随包复制。已安装 apply-film-knowledge 时，可通过 -KnowledgeTreeConfig 明确指定知识树的 .codex/knowledge-tree.json；既有有效桥接配置按安装器保留。安装不改 Vault 正文、关系或实践记录。

## 新建和继续项目

```text
使用 $start-ai-film-project，新建影视项目《项目名》。
```

启动器默认使用 v1.0 权威模板；产品 V1 以 workflowRelease: 1.0 标记，继续兼容 studio-v0.9 协议。启动器创建递增版本目录并交付轻量启动词。A/B/C1/C2/D/E 是六个专业部门，不预建六个永久聊天。说明当前故事、资产或片段即可开始；只有你明确要求新开或续接时才创建可见任务。

| 部门 | 责任 |
| --- | --- |
| A 制片统筹 | 目标、资源、进度、依赖与完成状态 |
| B 编剧开发 | 故事、人物、因果和对白 |
| C1 美术与资产 | 角色、空间、服化道、材质与状态 |
| C2 导演与视听制作 | 从前期贯穿整片的创作方向、表演、摄影灯光与节奏 |
| D 剪辑与声音后期 | 前期接口、真实组接、声音和影片完成 |
| E 发行与复盘 | 发行包装、资格、真实发布与反馈 |

C2 联合相关专业形成一份完整候选，不逐部门要求采用。候选完整稿自动保存；你明确采用后，由所属专业保存对应正式文本。继续工作从当前工作集恢复，不把整段历史聊天作为日常输入。

V1 项目中，你明确要求生成或编辑图片、输入充分且工具可用时，默认使用 Codex 原生图片能力；指定其他工具时按本次选择办理。确认方案不自动生成，失败不自动切换收费平台。视频生成保持原方式；无 V1 发布标识的旧项目不自动启用新分支。D 默认以达芬奇后台原生 API/MCP 作为后期主工具，只有接口实际可用且你已授权本次执行时，才在指定工程内操作；你仍可按本次选择手工完成。工具或画布链接按当次项目确认，不固定为全局网址。作品效果、声音与剪接以真实媒体验证；文本、接口和安装通过不能代替影片验证。

## 选题、影片参照与近期热度

可以直接使用 `$evaluate-film-topics` 判断已有故事的常用类型与《救猫咪》类型、查找可借鉴影片和评价选题。原来的故事骨架、剧本开发入口也可在当前对话按需调用它，不增加必经检查或新岗位。

查询“最近哪些内容热”时，默认只报告热榜、来源、地区、时间范围和读取限制，不自动给剧情或拍法建议；需要结合短片时再明确提出。支持抖音、B站、小红书、TikTok、YouTube，实际可读范围以当次验证为准。小红书正常登录会话与自动浏览器的连接问题暂缓解决，不把推荐页或第三方整理称为官方榜单。

## 达芬奇后期与调色

D 仍通过 `$edit-and-finish-ai-film` 统一接管和管理后期。`$grade-film-in-resolve` 负责影片意图、基础匹配、Look、必要局部与质感判断；`$davinci-resolve-mcp` 负责达芬奇原生技术接口，不代替创作决定。默认在同一工程中持续调整你指定的一版，只有明确要求测试时才制作原版与三个候选；方向按影片决定。

后台操作不占键鼠，不默认用外部 FFmpeg 调色代替原生工程。V0.9 的 `media_execution` 仍指工作室运行时内置媒体功能，保持原值；独立达芬奇调用按实际工具能力与本次执行授权办理，不自动迁移旧项目；V1 原生图片另按共同契约办理，视频生成、上传和发布权限不扩张。候选与真实导出不自动成为采用版本，正式采用后才由 D 更新对应记录。

## 生成视频整理与取用

生成视频下载到本地后，可以使用 `$organize-ai-film-media` 按片段整理多条完整原片、记录每条的可用区间并查找素材。明确尚未导入剪辑软件的新增文件可复制归位，已导入或状态不明的文件保持原位；不要求先标完所有区间。整理索引不代替 C 的准入或 D 的实际入片记录，不修改剪映工程、不预导出小片段。

## 版本与兼容

- 当前产品版本 V1（1.0），上一版 v0.9。框架与权威模板位于 [工作流](工作流/小陌AI影视工作流总框架_v1.0.md) 和 [V1模板](工作流/项目模板/小陌AI影视项目模板_v1.0/开始这里.md)。工作室协议保持 studio-v0.9；产品版本、运行时 schema 和影片目录的 vN 分别表示不同层次。
- 只有项目入口明确声明 workflowStudio: studio-v0.9 才使用工作室运行时；安装新 Skill 不会自动迁移旧项目。既有项目接入必须点名并保留其剧本、素材、编号和制作状态。
- 无此标识的 v0.8/v0.75/v0.7/v0.6/v0.5 项目保持自身入口及原有流程，不补建新目录或追溯重做已完成环节。
- v0.8 及更早版本、升级候选、个人备份与原始验收证据保留在原工作区，不随本仓库提供。
- S/A/V/CUT/PUB 编号语义不变。CUT、锁画、组件、Master/QC 和发行分别依赖实际证据，发布前仍须确认首映/投稿要求。

共同契约：[工作室工作方式](skills/orchestrate-ai-film-project/references/studio-v09-contract.md)；维护事实与验收见根 HANDOFF.md。日常项目无需读取维护历史。
