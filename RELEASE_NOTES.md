# V1.0 发布说明

## 六岗位启动维护更新｜2026-09-20

本次更新同步到 `main`，产品版本保持 1.0，工作室协议和 runtime schema 不变。旧 `v1.0.0` 标签保留首次发布内容，不移动标签；需要本次修复请使用最新 main。

### 修复内容

- V1 新建项目恢复“一段完整启动词”：当前任务作为 A，查询并复用或补齐 B/C1/C2/D/E 五个本地独立待命任务。
- 增加 `get_role_launch.ps1 -ProjectRoot <完整绝对路径>`，支持已有 V1 项目只读准备同一启动包；不再次建档、不修改项目、不接管旧工作集。
- V1 JSON 返回 `launchPromptVersion=4`、五段 `standbyPrompts` 和六个 `roleTitles`；保留 `departmentPrompts`，v0.9/v0.8 原有输出不变。
- 统筹入口、共同契约、V1 模板及维护规则与该行为对齐。空的岗位任务不走工作集交接，前期咨询不恢复旧 A 派岗门禁。
- 完整绝对路径校验拒绝依赖当前目录的 `D:relative` 与依赖当前盘符的 `\relative` 写法。

### 使用说明

README 补充适用场景、六部门交付、安装／建档／建岗三种动作、项目目录与候选／采用／实际制作状态。新增[六岗位启动与排障](docs/六岗位启动与排障.md)，说明已有任务复用、工具缺失、列表未返回新任务及工作集续接的处理。

这次同步不发布本机任务服务配置。自动建岗依赖 Codex 当前会话的原生工具，安装 23 个影视技能不等于任务工具、图片模型或剪辑软件已经接通。

### 验证与已知限制

- 本地正式修复：脚本行为 64/64；独立前向检查 37/37，绝对路径问题修复后针对性复验 3/3；隔离安装、正式安装及 23 项源码／安装哈希对照通过。
- 本次发布副本：复核同步文件与正式源一致、PowerShell BOM、文档本地链接与个人资料排除范围；重新执行隔离安装／VerifyOnly 和 V1 新建、已有项目只读启动、v0.9 兼容启动。
- 一次真实执行已创建五个岗位并收到“已待命”，逐个直读核对标题和项目目录通过。任务列表未返回这些新条目，因此列表归属、自动复用及重复启动防重仍未完整实测通过；不得据此重复创建。
- 没有执行媒体生成、剪辑、调色、导出或影片发布。软件检查不证明真实影片效果。

## V1.0 首次公开发布

发布日期：2026-09-20。

本次将正式版整理为独立 GitHub 发布副本。产品版本 1.0；工作室协议与运行时 schema 保持不变。保留 23 个技能及 V1/v0.9 模板，默认新建 V1；既有项目不自动迁移。

随包技能、安装器、框架和既有模板文件与正式源逐文件一致。分发适配仅涉及根文档、Git 配置与模板空目录占位。

## 本次打包验证

- 从本地 Git 仓库重新克隆：274 个跟踪文件逐字节一致，全部模板目录与 PowerShell UTF-8 BOM 保留。
- 23 个随包技能的结构校验全部通过。
- 对检出副本完成隔离安装、`VerifyOnly` 与重复安装；23 个已安装技能校验通过。
- 使用隔离的 `CODEX_HOME`、`USERPROFILE`、安装位置和项目目录执行测试，未改本机正式安装配置。
- 使用 `-NoOpen` 与显式隔离模板/项目路径，完成 V1 新建、同名版本递增以及 v0.9 模板启动。
- `项目/` 只保留 `.gitkeep`，本地项目内容命中忽略规则；未纳入维护备份、真实素材、知识库或本机配置。
- 对发布文件完成常见凭据模式扫描，未命中；正式源文件与打包前基线保持一致。

测试中发现安装器要求 `项目/` 目录存在，现以占位文件保留，重新克隆后的完整测试已通过。模板空目录同样通过占位文件保留，未修改启动器或安装器。

上述验证针对分发文件、安装与启动行为，不代表真实媒体质量、外部软件连接或整片制作效果已通过验收。
