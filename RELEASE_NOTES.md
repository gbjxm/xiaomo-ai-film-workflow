# V1.0 发布说明

发布日期：2026-09-20。

本次将正式版整理为独立 GitHub 发布副本。产品版本 1.0；工作室协议与运行时 schema 保持不变。保留 23 个技能及 V1/v0.9 模板，默认新建 V1；既有项目不自动迁移。

随包技能、安装器、框架和既有模板文件与正式源逐文件一致。分发适配仅涉及根文档、Git 配置与模板空目录占位。

## 本次打包验证

- 从本地 Git 仓库重新克隆：274 个跟踪文件逐字节一致，全部模板目录与 PowerShell UTF-8 BOM 保留。
- 23 个随包技能的结构校验全部通过。
- 对检出副本完成隔离安装、`VerifyOnly` 与重复安装；23 个已安装技能校验通过。
- 使用隔离的 `CODEX_HOME`、`USERPROFILE`、安装位置和项目目录执行测试，未改本机正式安装配置。
- 使用 `-NoOpen` 与显式隔离模板/项目路径，完成 V1 新建、同名版本递增以及 v0.9 模板启动。
- `项目/` 只保留 `.gitkeep`，本地项目内容命中忽略规则；未纳入维护备份、真实素材、知识库或本机配置。
- 对发布文件完成常见凭据模式扫描，未命中；正式源文件与打包前基线保持一致。

测试中发现安装器要求 `项目/` 目录存在，现以占位文件保留，重新克隆后的完整测试已通过。模板空目录同样通过占位文件保留，未修改启动器或安装器。

上述验证针对分发文件、安装与启动行为，不代表真实媒体质量、外部软件连接或整片制作效果已通过验收。
