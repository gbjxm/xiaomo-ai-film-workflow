# B_故事剧本

> V0.9：先读项目能力。候选按独立工作集 SaveDraft；明确采用且具备事务能力时由本专业准备完整文本经 Adopt 提交。采用输入先 Resolve，再读取不可变快照；本文件是可重建视图。能力关闭时只做候选演练，不调用旧单岗缓存或写回步骤。


> 工作流版本：v0.9。

> 本文件只由 B_故事剧本维护。对话中的候选、开放问题和 AI 推断写回时必须映射为待定；项目持久状态只用待定、采用、停用。

## 当前创作重点

当前任务：

当前剧本版本：

本轮只处理：

## 采用

-

## 待定

-

## 停用

-

## 故事核心

原始种子：

最想保留的直觉：

主角与关系：

主角想要什么：

阻力与冲突：

变化方向：

题材、背景与规则：

主题或情绪方向：

结尾方向：

## 当前剧本正文

正文载体：`B_故事剧本.md` 当前正文 / 外部 Word 精确指针

当前精确路径 / 版本 / SHA-256：

> 活跃区只保留当前正文或精确指针，不嵌入多版历史全文；旧稿进入 `90_制作资料/剧本/历史/` 且默认不读取。

当前正文：仅 Markdown 模式在此保留一份现行全文；Word 模式只保留上面的精确指针。

-

## Word 协同精修

启用状态：未启用

启用范围：正式对白 / 场景动作 / 故事层场记与连续性 / 计时 / 最终文字精修

活动 Word 路径 / 版本 / SHA-256：

最近保存基线版本 / SHA-256：

用户未同步修改：未知；Codex 不得声称已读取未保存内容。

Codex 修订或批注副本路径 / 基线版本 / SHA-256：

待合并差量与重叠冲突：

合并候选路径 / 版本 / SHA-256：

采用源稿路径 / 版本 / SHA-256 / 采用证据：

生产稿路径 / 版本 / SHA-256 / 状态：未建立；可选，不自动替代采用源稿。

场次稳定锚点与 `Sxx` 映射状态：

历史策略：旧工作稿与候选移入 `90_制作资料/剧本/历史/`，默认不读、不自动删除；具体保留深度由项目决定。

## 可进入制作的段落

只有段落被明确采用进入制作时，才由 B 分配 `S01`、`S02` 等稳定 ID。

| 段落 ID | 内容/作用 | 状态 | 版本 | 制作需要 | 关联资产 |
| --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |

## 制作启动判断

第一批准备制作的内容是否清楚：

当前待定项是否会让这批资产整体作废：

用户是否同意边制作边调整：

当前判断：未判断

## B_STAGE_READY v1｜专业自检与阶段完成包

当前状态：未检查；只有所有必需项通过且阻塞项为 0 才可写 `PASS`。

自检基准：本文件当前专业检查区与精确采用剧本工件；本文件定稿后再计算最终 SHA-256。

检查所绑定的采用剧本工件路径 / 版本 / SHA-256：

用户创作采用证据 / B 记录写入授权 / 进入制作范围：

创作专业检查：核心因果、人物目标与选择、关系结果、结构兑现、场景变化、结局与主题表达。

正文精修检查：目标场次、场景标题与锚点、对白职责与潜台词、可见动作、前后场连续性、故事层场记、节奏与必要声音信息。

制作检查：AI 可见/可听表达、人物/时间/空间/状态连续性、明确 `Sxx` 及版本、制作边界、计时证据状态、待定项影响分类。

证据检查：B/Word 当前 hash、采用与写入证据、Sxx 范围、阻塞项、非阻塞待定项、不可改变事实、推荐 C 第一批。

阻塞项数量 / 明细：

非阻塞待定项 / 影响：

阶段完成包生成状态：本文件定稿并计算 SHA-256 后，另行生成 `90_制作资料/剧本/阶段完成包/B阶段完成包_vNN.json`；B 提交前和 A 验收时都运行同一个只读验证器。完成包不得再回写本文件形成自哈希循环。

A 只做完整性审查：检查缺项、版本/hash、用户决定、阻塞项、Sxx 范围与任务包络；不重复上述创作细节审查。

## 最近交接

本次完成：

已经写入：

仍然待定：

发现的问题：

建议下一步：

需要 A 判断：
