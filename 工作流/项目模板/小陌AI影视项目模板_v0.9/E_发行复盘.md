# E_发行复盘

> V0.9：先读项目能力。候选按独立工作集 SaveDraft；明确采用且具备事务能力时由本专业准备完整文本经 Adopt 提交。采用输入先 Resolve，再读取不可变快照；本文件是可重建视图。能力关闭时只做候选演练，不调用旧单岗缓存或写回步骤。

> E 前期可咨询受众与资格限制；下方精确 QC Master 门只约束真实发行包装与发布就绪，不用于阻断前期咨询。


> 本文件只由 E_发行复盘维护。E 只从 D 交付、A 验收的精确 QC Master 开始，负责平台包装、最终封面、发行就绪检查、人工发布回执与真实数据复盘；不修改或派生成片，也不修改上游创作文件。

## 当前发行任务

当前任务：

关联 CUT 精确版本：

Picture Lock 状态及绑定版本：

关联 Master 版本：

Master 真实路径：

Master SHA-256：

Master Output Profile：

PIC / AUD / TEXT / GFX 版本与状态：

Master 精确绑定表证据路径与 SHA-256：

自动检查与人工完整观看/核听证据路径：

限制 / 恢复 / 权利证据路径：

目标平台：

目标账号代称：

保留首映或投稿资格：未确认

本轮只处理：

本次不做：

完成标准：

## 发行表达基线

目标受众：

账号语气：

必须保留：

避免事项：

## PUB 索引

由 `$package-video-release` 在平台专属包装被明确采用后分配 `PUB01`、`PUB02` 等稳定 ID。一个 PUB 只对应一个采用 CUT 精确版本、一个 Master 基线、一个平台和一个主要传播承诺。

| PUB ID | 状态 | 平台 | 关联 CUT | Master 版本/hash/Profile | 组件基线 | 包装决定 | 封面版本 | 最终包版本 | 就绪结论 | 发布回执 | 复盘版本 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
|  |  |  |  |  |  |  |  |  |  |  |  |

## 当前发布包

主要传播承诺：

包装决定路径与版本：

包装决定 SHA-256：

最终标题：

最终正文：

标签或分类：

最终封面路径：

最终封面 SHA-256：

当前包绑定的 Master 版本 / SHA-256 / Output Profile：

当前包绑定的 PIC / AUD / TEXT / GFX 版本与状态：

最终发布包路径与版本：

最终发布包 SHA-256：

发行检查结论：

检查输入指纹：

唯一 manual-copy 位置：

失效状态与原因：

## 成片版本请求

请求类型：新画幅 / 重编码 / 烧录字幕或字幕版 / 音轨版 / 成片修复 / 无

当前 Master 版本与 SHA-256：

目标 Output Profile：

请求原因：

路由状态：未触发 / 已交 A / A 已派 D / D 已交新 Master / A 已验收并恢复 E

恢复对象与精确版本：

恢复条件：

## 发布回执

公开链接或平台回执：

实际发布时间：

回执核验：

实际发布字段与最终包差异：

## 结果复盘

数据时间窗：

已核验事实：

样本限制：

主要假设：

替代解释：

下一次只调整的一组变量：

建议返回岗位：

## 最近交接

本次完成：

已经写入：

仍然待定：

发现的问题：

建议下一步：

需要 A 判断：
