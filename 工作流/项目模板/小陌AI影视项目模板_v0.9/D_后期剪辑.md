# D_后期剪辑

> V0.9：先读项目能力。候选按独立工作集 SaveDraft；明确采用且具备事务能力时由本专业准备完整文本经 Adopt 提交。采用输入先 Resolve，再读取不可变快照；本文件是可重建视图。相应工作集或采用事务能力关闭时，对应文本保存与采用只保留候选演练，不调用旧单岗缓存或写回步骤；独立达芬奇动作另按下述真实接口与本次授权判断。

> D 可在前期就节奏、覆盖、声音及交付接口提供受限意见，不要求先接管 C 媒体。后期默认采用达芬奇后台原生 API/MCP；实际工具可用且用户已授权本次执行时才操作指定工程，不占键鼠。用户仍可按当次选择手工完成。

> D 保持 `edit-and-finish-ai-film` 一个主入口，按需调用 `grade-film-in-resolve` 专业调色与 `davinci-resolve-mcp` 技术接口。`media_execution` 仍指工作室运行时内置媒体功能，保持项目原值；独立达芬奇工具不因此自动获权，不扩大 C 生成、上传或发布。


> 本文件只由 D_后期剪辑维护，并且只保留以下五个业务区块。详细逐镜、素材使用、派生、声音、字幕、VFX、Master 绑定和 QC 表放入版本化 `90_制作资料/后期方案/`。C 只给素材准入意见；D 负责精确接管、入片、CUT、完成和影片本体 QC。任何真实剪辑仍需独立执行授权。

## 1. 当前任务与接管快照

当前任务：

任务深度：单点纠错 / 局部方案 / 完整后期 / 执行交接 / 条件性后期预设

执行授权：未授权 / 已授权（范围：） / 等待人工执行 / 已完成 / 失败

关联 S/A/V：

目标输出：

后期预设：默认后期 Profile / 后期预设卡 vNN（路径 / hash）

C 交 D 素材清单：路径 / 版本 / SHA-256 / 当前有效性

D 接管快照：vNN / 路径 / 状态

工程版本与路径：

原生执行与续调：达芬奇版本 / 精确工程与时间线 / 实际 API 或 MCP 能力 / 本次授权范围 / 恢复点；默认同一工程、用户指定的一版持续调整，明确测试才保留原版与三个候选。未验证项保留未知，不据安装宣称全部 API 可用。

滚动组接 / 参考导出：路径 / 版本 / SHA-256 / 仅作参考

真实媒体核验：路径、hash、可打开性、批次、代理/派生关系

当前阻塞与未决项：

## 2. CUT、采用与 Picture Lock

> 首个由 D 使用真实媒体形成、真实可播放且可比较的粗剪即分配待定 `CUTxx`；用户滚动组接与 C 预演不占 CUT。CUT 创建、采用与锁画分别确认。

| CUT ID | 版本 | CUT 状态 | 真实视频路径 / SHA-256 | 方案文件 | 精确素材使用表 | Picture Lock | 当前有效性 / 问题 |
| --- | --- | --- | --- | --- | --- | --- | --- |
|  |  | 待定 / 采用 / 停用 |  |  |  | 未确认 / 已确认并绑定本版 / 已失效 |  |

当前权威 CUT：

当前采用证据：

当前 Picture Lock 证据：

版本变化：无 / 同一功能内顺序、切点、时长或采用媒体变化，`CUTxx` 升 `vNN` / 剪辑方向根本改变，新建 CUT

差量接管与 Reconform：

返回 C 的功能性缺口：CUT 时间点 / S/V / 缺少功能 / 不可替代理由 / 最小责任站 / A 路由

## 3. 专业分支与组件状态

> PIC/AUD/TEXT/GFX 只用 `未触发 / 进行中 / 已冻结 / 已失效 / 不适用＋原因`。没有触发的详细表不展开；组件变化不自动提升 CUT。

> 调色先核对输入/色彩管理、影片意图与基础匹配，再按需设计 Look、局部和质感，动态复看并原生导出回读；不固定参数、风格或每镜全套操作。调色工作稿与测试结果保留候选身份，正式采用后 D 才更新对应 PIC/调色指针及实际受影响的 Master/QC 依赖；专项和工具技能不创造 CUT 编号。

| 组件 | 版本 | 状态 | 绑定 CUT / Lock | 文件路径 | SHA-256 | 证据或不适用原因 |
| --- | --- | --- | --- | --- | --- | --- |
| PIC |  |  |  |  |  | VFX / Online / 色彩管理 / 匹配 / Look / 画面 QC |
| AUD |  |  |  |  |  | DX / ADR / VO / 环境 / 拟音 / FX / MX / M&E / Final Mix |
| TEXT |  |  |  |  |  | 普通字幕 / 翻译 / SDH / Forced Narrative |
| GFX |  |  |  |  |  | 画外标题 / 片头片尾 / 署名 / 图文 |

临时 DX/FX/MX：未使用 / 已使用（版本与用途）；不得冒充冻结 AUD

当前组件失效传播：只失效受影响组件 / Master / 对应 QC；PIC 或 CUT 是否受影响及依据

来源、许可、真人声音同意、AI 状态与未知项：证据表路径 / 结论

## 4. Master、QC 与恢复

> Master 必须精确绑定当前 CUT 与四组件；旧声音、字幕、调色或 GFX 不得静默混入。自动检查与人工完整观看/核听缺一不可。

Master：vNN

真实路径：

SHA-256：

Output Profile：

精确绑定：CUTxx vNN / PIC vNN或N/A原因 / AUD vNN或N/A原因 / TEXT vNN或N/A原因 / GFX vNN或N/A原因

绑定有效性：有效 / 已失效（变化输入与传播范围：）

自动检查：通过 / 不通过 / 未检查；工具、日期、原始证据路径

人工完整观看/核听：通过 / 不通过 / 未检查；人员、日期、播放条件、证据

返修后重新输出与回归：不适用 / 已完成 / 未完成；新 Master 与证据

已知限制与残余风险决定：

恢复位置：工程 / 精确采用媒体 / 字体插件LUT / 其他依赖

恢复验证：重开工程 / 重连精确采用媒体 / 再次输出；结果、日期、输出 hash

D 完成门：未达到 / 已达到（采用 CUT vNN＋Lock＋组件状态＋Master path/version/hash/Profile＋自动/人工 QC＋限制决定＋恢复＋权利证据）

## 5. 最近交接

本次完成：

已写入的精确文件与版本：

真实执行与测试证据：

仍然待定或阻塞：

给 A 的完成证据：

建议下一步：继续 D / 经 A 返回 C / 交 A 验收 / 等待用户决定 / 等待独立执行授权

明确未授权或未执行：
